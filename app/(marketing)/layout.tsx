export default function MarketLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <section>
            MarketLayout
            {children}
        </section>
    );
}
